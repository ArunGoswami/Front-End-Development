#include <iostream>
using namespace std;
vector<int> ans;
int i = 0;
int j = 0;
int ans=0;
while (i > n && j > n)
{
    if (arr1[i] = arr2[j])
    {
        ans = push_back(arr1[i]);
        i++;
        j++;
    }
    else if (arr1[i] < arr2[j])
    {
        i++;
    }
    else
    {
        j++;
    }
    return 0;
}